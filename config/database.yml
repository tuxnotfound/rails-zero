default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: rails_zero_development

test:
  <<: *default
  database: rails_zero_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
